# Logbook — Independent live-status re-check, open items restated, docs sync

**2026-09-03, ~15:43 CEST** (`date -u` 13:43:09, +2h)

A separate session (not the one that built P0-P7 or stood up the systemd
service in `LOGBOOK_09032026_142903.md`) asked for live proof the engine
was actually running, with the raw command output, not a status claim.
Re-ran the same 5 checks independently, from a fresh SSH connection
through the CX23 jump host, against the persistent service already
running from the prior session. Recorded here because independent
re-verification is itself worth a record, separate from the session that
first stood the service up.

## Live-check results (raw)

**1. Process alive:**
```
sensalis 1123475  0.4  3.9 2141248 644492 ?      Ssl  12:24   0:20 /home/sensalis/work/knowledge_engine/.venv/bin/python3 /home/sensalis/work/knowledge_engine/.venv/bin/uvicorn api.main:app --host 127.0.0.1 --port 8000
```

**2. Port listening:**
```
LISTEN 0      2048                     127.0.0.1:8000       0.0.0.0:*    users:(("uvicorn",pid=1123475,fd=13))
```

**3. Health check:**
```
HTTP/1.1 200 OK
content-type: application/json

{"status":"ok"}
```

**4. Real query** (`GET /search?q=python%20decorator&k=5`) — `200 OK`, 5
real hits with genuine BM25/vector/RRF scores, e.g.:
```json
{"query_id":4,"query":"python decorator","hits":[
 {"doc_id":"https_docs_python_org_3_library_sqlite3_html_cf11cb89","rank":0,"bm25_score":3.4747,"vector_score":0.6279,"rrf_score":0.0323},
 {"doc_id":"doc07_fastapi","rank":1,"bm25_score":2.7652,"vector_score":0.5613,"rrf_score":0.03125},
 {"doc_id":"doc18_ubuntu_2404","rank":2,"bm25_score":3.1964,"vector_score":0.5606,"rrf_score":0.0310},
 {"doc_id":"https_en_wikipedia_org_wiki_Tf_E2_80_93idf_245bc05f","rank":3},
 {"doc_id":"doc13_python_venv","rank":4,"bm25_score":3.5014,"vector_score":null,"rrf_score":0.0164}
]}
```

**5. Uptime:**
```
PID     ELAPSED CMD
1123475 01:16:02 .../uvicorn api.main:app --host 127.0.0.1 --port 8000
```
1h16m at check time (started ~12:24 UTC same day, i.e. shortly after the
prior session's systemd deployment) — no unexplained restart.

**Same PID (1123475) as the prior session's deployment** — this confirms
the service handed off cleanly from that session's `systemctl --user
enable --now` to being reachable and correct from an entirely separate
session/connection later, which is the actual point of a persistent
service (survives beyond the session that started it).

## Documentation sync (this session)

Per the operating convention, updated the repo's docs to state current
status honestly rather than letting them drift from what's actually true:

- **`README.md`**: added a "live-verified" timestamp+result line to the
  Status paragraph pointing at this logbook; added a note on the P7
  citation-accuracy roadmap line flagging the 3-query sample as small.
- **`HELP.md`**: added an explicit "what it takes to unblock the 4
  remaining P5 connectors" paragraph (Azure AD app registration in a real
  tenant, specific Graph permission scopes, `KE_MSGRAPH_*` env var
  pattern, same per-document ACL-tagging approach the Git connector
  already uses) — the live-check commands and the P3 unblock path
  (`/select` capture -> `ltr.status` -> 500-interaction gate) were already
  present from the prior session's update and didn't need duplicating.
  Added the same small-sample caveat as README to the P7 section.

**Explicitly restating the two open items so they don't get rounded up
in a summary:**

- **P3 is BLOCKED, not "in progress."** `ltr.status` reports `BLOCKED
  (0/500 interactions)` — this system has had no real usage yet, only
  smoke-test/verification queries (which are exactly the kind of traffic
  that shouldn't be trained on, since it's not representative of real
  usage patterns). Nothing about today's live-check queries changes this
  number in any way that matters — they're the same kind of synthetic
  verification traffic, not real usage.
- **P5's SharePoint/OneDrive/Outlook/Teams connectors remain unbuilt.**
  `access.connectors` still reports `NOT_CONFIGURED` for all four, for the
  same reason as every prior report: no Microsoft Graph API credentials
  for a real tenant exist for this project. This is a hard external
  dependency, not something more engineering time on this repo can close
  on its own.

**P7's finding, restated precisely (not rounded either direction):**
automated `check_citations()` = 3/3 (no hallucinated citations); manual
side-by-side spot-check against source text = 2/3 fully correct, 1/3 with
a real misattribution (correct chunk, attached to the wrong claim). Small
sample size (3) — sufficient to prove the misattribution failure mode is
real, insufficient to state a reliable accuracy rate. A larger manual
validation pass is recommended before this number is used to justify
citation trust at any scale beyond this demo corpus.

## Status after this session

No code changed. Confirmed independently and honestly that "the code is
built" and "the engine is running" are still both true, from a fresh
vantage point separate from the session that first proved it. Docs now
carry an explicit live-verification timestamp + pointer, and the two open
items (P3 blocked, P5 partial) are restated in this entry as still-open,
not resolved.
