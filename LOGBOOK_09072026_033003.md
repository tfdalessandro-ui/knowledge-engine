# Logbook — auto_tune.py run (2026-09-07 03:30 UTC)

Automated recurring GA search (`scripts/auto_tune.py`, cron `AUTO_TUNE`).
Corpus/judgment-set content changed since the last run (hash differs), so
this ran a real search rather than skipping.

## Setup
- 126 total queries: 106 tuning pool, 20 permanent holdout (deterministic hash-based split, stable across runs).
- GA evaluated 243 distinct genomes on the tuning pool only.

## Result
- Tuning-pool (in-sample) champion: `{'fusion_mode': 'weighted', 'rrf_k': 37, 'chunk_fanout': 35, 'alpha_mode': 'fixed', 'alpha': 0.552, 'alpha_base': 0.745, 'alpha_slope': 0.078}` -- rel. improvement +1.91%
- **Held-out (never used for tuning) -- champion: +4.56% | currently deployed: +4.38%**
- Deploy margin required: +0.50%
- Decision: NO DEPLOY (champion did not beat the deployed config by enough on held-out data)


## Currently deployed config (baseline for this comparison)
`{'fusion_mode': 'weighted', 'rrf_k': 1, 'chunk_fanout': 100, 'alpha_mode': 'fixed', 'alpha': 0.535, 'alpha_base': 0.535, 'alpha_slope': 0.0}`

## Champion config
`{'fusion_mode': 'weighted', 'rrf_k': 37, 'chunk_fanout': 35, 'alpha_mode': 'fixed', 'alpha': 0.552, 'alpha_base': 0.745, 'alpha_slope': 0.078}`
