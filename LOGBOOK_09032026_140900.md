# Logbook — Crawler scheduled, allowlist grown, merge paused mid-run (session stop)

**2026-09-03, ~16:09 CEST** (`date -u` ~14:09, +2h)

Session paused by the user mid-task ("stop and log now, we resume
later"). This entry records exact state so the next session can pick up
without re-deriving it.

## What was asked, in order

1. Schedule the P6 crawler to run daily (previously a one-time manual
   batch, confirmed in `LOGBOOK_09032026_134309.md`'s sibling report --
   no cron/systemd timer existed for it).
2. Mid-task: "the whole system must run crawling and discovery 24/7."
   Pushed back rather than complying literally: the 5-URL allowlist is
   the entire discovery surface by this project's own design (`HELP.md`:
   "the allowlist IS the deliverable... scope drift is a legal/ToS
   exposure risk"), so looping continuously over the same 5 fixed pages
   doesn't discover anything new -- it just re-fetches unchanged content
   and risks looking like abusive traffic against Wikipedia/sqlite.org/
   docs.python.org. Asked the user to choose: grow the allowlist, tighten
   the schedule, or this meant a different (Sensalis) system entirely.
   **User chose: grow the allowlist.**
3. Mid-task: "C: add it" -- referring to item C from the prior report
   (a "research-to-production loop, 2-week cycle"), which that report
   confirmed does not exist anywhere in this project (grepped the repo,
   README, HELP -- zero matches). **This request is still open and
   UNADDRESSED** -- it needs scoping with the user before building
   anything (what "research" means, what "production" transition means,
   safety guardrails on an autonomous cycle) rather than inventing a
   design for an unspecified autonomous subsystem. Do not build this
   without that conversation.

## What was actually done, in order

1. **Cron added** (unprivileged `sensalis` crontab, no sudo needed --
   confirmed backed up first: `~/crontab.node.bak.20260903_140404`):
   ```
   15 2 * * * cd /home/sensalis/work/knowledge_engine && PYTHONPATH=src /home/sensalis/work/knowledge_engine/.venv/bin/python -m crawl.pipeline >> /home/sensalis/work/knowledge_engine/log/crawl_cron.log 2>&1  # KE_CRAWL_DAILY
   ```
   Runs daily at 02:15 UTC. **Not yet changed to hourly** -- the user's
   answer ("grow the allowlist") didn't ask for a schedule change, so
   daily stands. If a tighter interval is wanted later, revisit
   `KE_CRAWL_DAILY`'s cron line.

2. **Allowlist grown from 5 to 12 URLs** in
   `src/crawl/allowlist.py`, each new entry checked before adding (not
   assumed permissive):
   - Existence verified live (`curl -A "<the crawler's own declared UA>"`,
     all 7 returned `200`).
   - Robots.txt coverage reused from the already-reviewed domain-level
     checks (Wikipedia `/wiki/<Article>` allowed, `docs.python.org
     /3/library/` allowed, `sqlite.org` page not in its excluded dirs) --
     same pattern as the original 5 entries, not a fresh per-domain
     robots.txt re-read since these 3 domains were already checked.
   - New entries, each with a stated on-topic reason (same curation
     discipline as the original 5 -- matched to this project's own
     phases, not generic filler):
     `Knowledge_graph` (P4), `Learning_to_rank` (P3),
     `Named-entity_recognition` (P4), `Large_language_model` (P7),
     `Vector_database` (P2), `docs.python.org/3/library/venv.html`
     (this repo's own setup), `sqlite.org/wal.html` (background reading
     tied to a real thread-safety note already in `HELP.md` -- checked
     the code directly first: grepped for `journal_mode`/WAL usage in
     `src/`, found none, so the reason text was corrected to describe
     this as background reading on a relevant concurrency mode, not a
     claim this project's stores are actually in WAL mode -- they
     aren't, confirmed by the grep, not assumed).

3. **Ran `crawl.pipeline` against the 12-URL allowlist:**
   ```
   fetched=7 skipped_duplicate=5 skipped_robots_disallowed=0 errors=0
   elapsed=14.3s budget=60s -> WITHIN BUDGET
   ingest: scanned=12 added=7 updated=0 unchanged=5
   ```
   All 7 new pages fetched cleanly into `data/crawled/` / the separate
   `data/web_tantivy_index/` (not yet the main index at this point).

4. **Started the merge into the main corpus** (same procedure as the
   documented merge-trap fix: copy files into `data/corpus/` itself, then
   re-run `ingest.pipeline` with no `--corpus` override -- NOT pointing
   `run_ingest` at `data/crawled/` directly, which would delete every
   existing doc not in that 7-file directory):
   ```bash
   cp data/crawled/*.html data/corpus/
   # data/corpus/ now has 47 files (40 + 7)
   PYTHONPATH=src .venv/bin/python -m ingest.pipeline
   ```

## STATE AT STOP -- incomplete, resume here

**`ingest.pipeline` was still running (embedding the 7 new documents)
when the user asked to stop.** Last captured output before pause:
```
scanned=... (not yet printed -- run had gotten past corpus scan and the
BGE-Small embedding model load: "Loading weights: 100%|...| 199/199")
```
`data/corpus/` = 47 files. **The live index has NOT yet been updated** --
the main `data/tantivy_index/`/`data/faiss_index/` still reflect the
pre-merge 40-document state as of this pause; a `/search` query right now
would not find content from the 7 new pages, and the live `/search`
service (PID 1123475, still running) has not been restarted, which it
will need to be after the merge completes (FAISS vector index has no
reload path -- see `LOGBOOK_09032026_142903.md` for why this matters).

**The remote `ingest.pipeline` process itself was left running** on
sensalis-node rather than killed -- it's not destructive to let it finish
on its own (same idempotent/resumable pattern as every other ingest run
in this project), and killing it mid-embed would just mean re-doing that
work next session. **Next session should, in order:**
1. Check whether the background `ingest.pipeline` invocation finished
   (`ps aux | grep ingest.pipeline` on the node, or just check whether
   `data/tantivy_index/`'s doc count is now 47 via a quick
   `eval.run --index real`).
2. If finished cleanly: restart the `knowledge-engine.service` systemd
   unit (`systemctl --user restart knowledge-engine.service`) so the live
   `/search` API picks up the new vectors, then verify live with a real
   query against one of the 7 new pages' content (e.g. `q=knowledge
   graph` or `q=learning to rank`).
3. If it died/was interrupted: safe to just re-run
   `PYTHONPATH=src .venv/bin/python -m ingest.pipeline` -- the SQLite
   content-hash registry means already-embedded docs won't be redone,
   only what's still missing.
4. Watch for the same margin-narrowing effect the P6 merge caused before
   (`LOGBOOK_09032026_142903.md`, Parts 4-6) -- 7 more crawled pages
   competing for top-k slots could shift P1/P2's baseline numbers again;
   re-run the eval harness and check before assuming nothing changed.
5. **"C: add it" (research-to-production loop) is still open and needs a
   scoping conversation with the user, not an assumed design** -- surface
   this explicitly when the session resumes rather than silently building
   something for it or silently dropping it.

## Why this belongs in the logbook now rather than after finishing

The user explicitly asked to stop and log the current state for a later
resume, mid-operation, with the main index genuinely out of sync with the
corpus directory (47 files on disk, 40 documents in the live search
index) until the paused `ingest.pipeline` run either completes or is
re-run. That's exactly the kind of state a future session (or a
different Claude Code session) needs spelled out plainly rather than
discovered the hard way by, e.g., assuming the merge already landed.

## Addendum — background ingest finished after the stop

The paused  run completed on its own after this session
was already stopped (no further commands issued, per the user's
instruction to stop and resume later):

Clean merge,  confirms none of the original 40 documents were
lost (the exact mistake this project hit once before, per
, did not recur here).

**Still not done, deliberately, pending resume:** the live
 has NOT been restarted, so  is still
serving the pre-merge FAISS vectors as of this addendum — step 2 in the
resume list above (restart the service, verify with a real query against
the new content, then check the eval-harness margin) is still the correct
next step when work resumes.

## Addendum — background ingest finished after the stop

The paused `ingest.pipeline` run completed on its own after this session
was already stopped (no further commands issued, per the user's
instruction to stop and resume later):
```
scanned=47 unchanged=40 added=7 updated=0 removed=0 vectors_added=159 compacted=False
```
Clean merge, `removed=0` confirms none of the original 40 documents were
lost (the exact mistake this project hit once before, per
`LOGBOOK_09032026_142903.md`, did not recur here).

**Still not done, deliberately, pending resume:** the live
`knowledge-engine.service` has NOT been restarted, so `/search` is still
serving the pre-merge FAISS vectors as of this addendum — step 2 in the
resume list above (restart the service, verify with a real query against
the new content, then check the eval-harness margin) is still the correct
next step when work resumes.
