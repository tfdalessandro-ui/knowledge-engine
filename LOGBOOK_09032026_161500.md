# Logbook — Session paused for unplanned node power-off; GA overfitting test mid-run

**2026-09-03, ~18:15 CEST** (`date -u` ~16:15, +2h)

User needs to physically unplug sensalis-node imminently. Logging current
state fast, before losing the connection, per the standing instruction to
always log findings (see memory `feedback-ke-always-log-findings.md`).

## Safety check: nothing write-critical in flight

The only thing running at the moment of this entry is
`ga_tune_hybrid_cv.py`, a **read-only** script (queries the live Tantivy/
FAISS index via `HybridIndex.search()` and `RealBM25Index.search()`, never
writes to any index/registry/DB). A hard power-off simply kills it
mid-run -- no corruption risk, nothing to clean up, safe to just unplug.
No `ingest.pipeline`, compaction, or any other write-path process is
active. The live `knowledge-engine.service` only serves read queries
(`/search`, `/health`); a hard stop there is also safe, the systemd unit
is `Restart=always` and will come back cleanly on reboot/power-on.

## What was in progress -- GA parameter tuning + overfitting check

1. **`ga_tune_hybrid.py`** (already completed, results known): a genetic
   algorithm over `HybridIndex`'s two exposed parameters (`rrf_k`,
   `chunk_fanout`), fitness = hybrid nDCG@10 on the full 30-query judgment
   set. **Result: default (rrf_k=60, fanout=100) scores -1.68% relative to
   BM25-only; GA-found best (rrf_k=1, fanout=61) scores +1.97%** -- a real
   improvement, but still short of the +5% exit criterion. Flagged before
   running the next step that `rrf_k=1` is a near-degenerate corner of the
   RRF formula and the tuning was done against the SAME judgment set used
   to measure it -- a real overfitting risk, untested at that point.

2. **`ga_tune_hybrid_cv.py`** (started, **NOT YET COMPLETE as of this
   entry** -- still running in the background when the unplug request
   came in): 5-fold cross-validation to test whether `(rrf_k=1,
   fanout=61)` actually generalizes. For each of 5 folds (~6 held-out
   queries each), runs a fresh GA search on the other ~24 queries, then
   scores both the fold's GA-selected genome AND the fixed default
   (60, 100) on the held-out fold -- queries the GA never saw during that
   fold's search. Read-only against the live index throughout, same
   safety profile as above.

**NO RESULT YET.** The background task had not completed when this entry
was written -- do not assume or report a cross-validation verdict until
the actual output is read. Its output file (on this session's local
machine, not the node) is:
`ga_tune_hybrid_cv.output` under this session's tasks directory, task id
`b8vzv1l9q`; the node-side log is `/home/sensalis/work/knowledge_engine/ga_tune_hybrid_cv.log`.

## Resume instructions for next session

1. Reconnect to the node once it's back on LAN/power
   (`ssh -i ~/.ssh/id_ed25519 -J root@157.90.229.194 -p 2222 sensalis@localhost`,
   or direct LAN `ssh -i ~/.ssh/id_ed25519 sensalis@192.168.178.65`).
2. Check `cat /home/sensalis/work/knowledge_engine/ga_tune_hybrid_cv.log`
   -- if the power-off killed it mid-run, the file will be incomplete or
   missing the `=== CROSS-VALIDATION SUMMARY ===` section; just re-run
   `cd /home/sensalis/work/knowledge_engine && .venv/bin/python
   ga_tune_hybrid_cv.py > ga_tune_hybrid_cv.log 2>&1` -- it's read-only
   and idempotent, safe to simply redo from scratch.
3. Once a real result exists, report it plainly (average held-out
   relative-improvement for the GA-selected genome vs. the fixed default,
   and whether the per-fold genomes were stable or scattered across the
   search space -- scattered genomes across folds would itself be
   evidence of overfitting even before looking at the held-out numbers).
4. Do NOT apply `(rrf_k=1, fanout=61)` (or any other GA-found genome) to
   the live `knowledge-engine.service`/`api/main.py` until the
   cross-validation result is in and reviewed -- that was the whole point
   of running this check before deploying anything.
5. Two temp scripts left on the node, both safe to leave or clean up
   later: `ga_tune_hybrid.py`, `ga_tune_hybrid_cv.py` (repo root, not part
   of `src/`), plus their `.log` files.

## Files/state as of this pause

- No files in `data/` changed this session (all read-only experiments).
- Two temp scripts + logs at the repo root (listed above), not yet
  cleaned up.
- `knowledge-engine.service` unchanged, still serving the compacted
  47-doc index with the DEFAULT `rrf_k=60, chunk_fanout=100` -- the GA
  finding has NOT been deployed anywhere.
