# Logbook — OSE rename: systemd units renamed one at a time, each with a regression check

**2026-09-16 (22:15 UTC).** Continues the gradual OSE rename. One change per step, each followed by a real check.

## Findings

- **My directory rename had broken the `KE_AUTO_TUNE` cron job.** Its line still ran `cd /home/sensalis/work/knowledge_engine`,
  which no longer exists. It would have failed at the next run (03:30 UTC). The script was already
  failing separately since 14 Sep because `adaptive_alpha` was missing, so it had not run successfully since
  then. Fixed before the next run.
- **Bug in the Sensalis clone, not fixed yet:** its `scripts/auto_tune.py` restarts `knowledge-engine.service`, which is
  this engine on port 8000, not the clone's own `sensalis-pricing-engine.service`. It was copied unchanged at clone
  time. Auto-tune isn't scheduled on the clone, so this never ran. Tracked in TODO item 11.
- `deploy/knowledge-engine.service` in the repo still had the old `~/work/knowledge_engine` paths. It is now
  `deploy/ose.service`, and a `cmp` confirms it matches the live unit exactly.

## Steps and checks

| Step | Check | Result |
|---|---|---|
| Crontab path | diff shows only 1 of 28 lines changed; `auto_tune.py` imports cleanly | pass |
| `ke-status-recheck` → `ose-status-recheck` | same next run time; manual run succeeded; `status_recheck.py` reports all checks passed | pass |
| `ke-research-loop` → `ose-research-loop` | same next run time; manual run succeeded, log line added | pass |
| `ke-research-crawl` → `ose-research-crawl` | same next run time; manual run succeeded | pass |
| `ke-kg-extract` → `ose-kg-extract` | real extraction run succeeded; KG still 215 entities / 9 relations; status recheck passes | pass |
| `ke-crawl` → `ose-crawl` | same next run time; manual run succeeded (`scanned=12`) | pass |
| `knowledge-engine.service` → `ose.service` | top-3 results for `bm25` unchanged; enabled at boot; old unit gone; `_engine_pid()` finds the new unit; the restart command `auto_tune.py` uses → health 200; status recheck passes | pass |

For each timer, the script copied its stamp file to the new name, so every timer kept its last-run time.
On the Sensalis clone, `/health` returns ok on port 8010, `vcn-offsite-backup-node.timer` is still enabled,
and the node has 0 failed units.
