# Logbook — KE_AUTO_TUNE first-scheduled-runs check (2026-09-06 09:23 UTC)

Verification pass (not an auto_tune run). Checks the first two scheduled
firings of the `KE_AUTO_TUNE` cron since it was installed.

## Verdict: healthy. Cron fired on schedule both days and correctly skipped.

## Cron firings
- Entry present: `30 3 * * *` -> `.venv/bin/python scripts/auto_tune.py` (# KE_AUTO_TUNE)
- `AUTO_TUNE_LOG.md` roll log:
  - 2026-09-05T03:30:02Z: skipped, no corpus/judgment change since last run
  - 2026-09-06T03:30:02Z: skipped, no corpus/judgment change since last run
- `log/auto_tune_cron.log`: two lines, both "No change detected -- skipped."
- No `LOGBOOK_0905*` / `LOGBOOK_0906*` from auto_tune — correct, the script
  only writes a logbook on a real (non-skipped) run.

## The skip is genuine, not a stuck-hash bug
Independently recomputed `_content_hash(settings)` on the node:
- recomputed: `75d7c3e1eceb8117878a433f761c1ea6a52872167ea8f73ca6c33b9e8b4d1fe4`
- stored in `data/auto_tune_state.json`: identical.
Corroborated by `log/crawl_cron.log` — every daily crawl reports
`ingest: scanned=12 added=0 updated=0 unchanged=12`. The corpus genuinely
has not changed since the last real run, so there is nothing to re-tune.

## Last real run (unchanged baseline)
2026-09-04 10:38 UTC (manual, pre-cron): held-out champion +5.15% vs
deployed +4.95%, margin required +0.50% -> NO DEPLOY. Deployed config
remains `weighted / rrf_k=1 / chunk_fanout=100 / fixed alpha=0.535`.

## Deploy path state
- No deploy has ever occurred, so `.env` does not exist and there are no
  `.env.bak_*` files. This is correct, not a gap: the recorded
  `deployed_genome` is exactly the code default in `src/config/__init__.py`
  (`hybrid_alpha=0.535`, `alpha_mode="fixed"`), so absent-.env == deployed.
- No rollback has been triggered.
- `_deploy()` restarts via `systemctl --user restart knowledge-engine.service`,
  which matches how the service actually runs (see below). Deploy path is
  correctly wired.

## Live service
`knowledge-engine.service` is a **systemd user unit**, not a system unit —
`systemctl is-active` at system scope reports "could not be found"; must use
`systemctl --user`. Noted so future checks do not misread this as a dead service.
- active (running) since 2026-09-04 06:14:03 UTC (~2 days), Main PID 1448736,
  395.5M RAM, 0 restarts. Listening on 127.0.0.1:8000.
- `GET /health` -> 200 `{"status":"ok"}`
- `GET /search?q=bm25%20ranking&k=3` -> 200, 3 real hits with doc_id + scores:
  `doc01_bm25` (rrf 0.9146), `https_en_wikipedia_org_wiki_Okapi_BM25_...` (0.8837),
  `https_sqlite_org_fts5_html_...` (0.8072).

## Observation (not a fault)
The tuner is idle by construction: the daily crawl is ingesting nothing new
(all 12 seeds duplicate/unchanged), so the content hash will stay frozen and
KE_AUTO_TUNE will keep skipping until the seed list is expanded or the
judgment set is edited. Expected behaviour, but worth knowing the GA will not
re-run on its own until corpus input actually changes.
