# Logbook — auto_tune.py run (2026-09-04 10:38 UTC)

Automated recurring GA search (`scripts/auto_tune.py`, cron `AUTO_TUNE`).
Corpus/judgment-set content changed since the last run (hash differs), so
this ran a real search rather than skipping.

## Setup
- 106 total queries: 88 tuning pool, 18 permanent holdout (deterministic hash-based split, stable across runs).
- GA evaluated 241 distinct genomes on the tuning pool only.

## Result
- Tuning-pool (in-sample) champion: `{'fusion_mode': 'weighted', 'rrf_k': 97, 'chunk_fanout': 33, 'alpha_mode': 'fixed', 'alpha': 0.586, 'alpha_base': 0.756, 'alpha_slope': 0.237}` -- rel. improvement +2.21%
- **Held-out (never used for tuning) -- champion: +5.15% | currently deployed: +4.95%**
- Deploy margin required: +0.50%
- Decision: NO DEPLOY (champion did not beat the deployed config by enough on held-out data)


## Currently deployed config (baseline for this comparison)
`{'fusion_mode': 'weighted', 'rrf_k': 1, 'chunk_fanout': 100, 'alpha_mode': 'fixed', 'alpha': 0.535, 'alpha_base': 0.535, 'alpha_slope': 0.0}`

## Champion config
`{'fusion_mode': 'weighted', 'rrf_k': 97, 'chunk_fanout': 33, 'alpha_mode': 'fixed', 'alpha': 0.586, 'alpha_base': 0.756, 'alpha_slope': 0.237}`
