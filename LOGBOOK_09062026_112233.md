# Logbook — Judgment set grown again (106→126 queries); status check on the recurring auto-tune first

**2026-09-06, ~13:22 CEST** (`date -u` ~11:22)

## Status check first

Before growing anything, checked the recurring `KE_AUTO_TUNE` cron
(`LOGBOOK_09042026_112500.md`)'s first two days live:

```
AUTO_TUNE_LOG.md:
2026-09-05T03:30:02Z: skipped, no corpus/judgment change since last run
2026-09-06T03:30:02Z: skipped, no corpus/judgment change since last run
```

Fired correctly both mornings at 03:30 UTC, correctly skipped both times
(content hash unchanged since the 2026-09-04 run) -- no wasted compute,
no logbook spam for a skip, exactly the intended behavior.
`auto_tune_state.json` still reflects the 09-04 run; no `.env` exists (no
deploy has happened); live service unchanged (PID 1448736, `/health`
200 OK). A separately-scheduled reminder task (local, this session's own
scheduling, unrelated to the cron itself) fired late due to the app being
closed at the intended time -- noted for completeness, doesn't affect the
node's own state, which was checked directly rather than trusted from
that report.

## Judgment set grown again: 106 -> 126 queries, 221 -> 246 pairs

User asked to grow the judgment set again. Every corpus doc_id already
had at least one judgment (checked first: `corpus_stems - judged_ids` was
empty), so this round added genuinely new query angles on already-covered
docs rather than closing a coverage gap -- 25 new pairs, each grounded in
content actually read (this session or an earlier one), verified
non-duplicate against the existing 106 queries before adding (listed all
existing queries first, checked against them by eye).

New coverage added:
- `doc01_bm25`: the k1/b parameter roles specifically (not just "what is
  bm25" generally).
- `doc16_rss_memory`: the physical-vs-swapped-vs-never-touched memory
  distinction specifically.
- `doc17_latency_percentiles`: why percentiles over a plain average.
- `doc03_ndcg`/`doc04_mrr`: a genuine head-to-head comparative query
  between the two metrics, not yet asked this way.
- `doc05_recall`/`doc21_precision`: the "return everything" vs
  "return one confident result" extremes specifically (from the docs'
  own stated examples).
- `doc20_web_crawling`: the crawling-vs-ranking separation-of-concerns
  point specifically.
- `doc26_hnsw`: the exact-recall-vs-speed tradeoff and coarse-to-fine
  search mechanics specifically (not just "what is hnsw").
- Okapi_BM25 wiki page: BM25F's document-structure/anchor-text handling,
  and the historical City University London origin -- both read directly
  from the page's own text earlier this investigation, not inferred.
- Tf-idf wiki page: TF-PDF and Delta TF-IDF derivatives specifically,
  same basis.
- sqlite FTS5 page: the `bm25()` column-weighting arguments and the
  `highlight()` function specifically.
- Python sqlite3 stdlib page: `executemany()` batch inserts and
  placeholder-based SQL-injection avoidance -- both read directly from
  this page's own content when it appeared in an earlier live `/search`
  response this investigation captured.

Backed up first: `data/judgments/judgments.json.bak_pre_grow2_20260906`.
Verified: `test_harness.py::test_judgment_set_size` (and the other 3
tests in that file) pass -- 246 pairs is within the 100-350 range, 126
queries clears the 100 floor, both set 2026-09-04.

## What happens next

This change alters the corpus/judgment content hash, so **the next
`KE_AUTO_TUNE` cron fire (03:30 UTC tomorrow) will no longer skip** -- it
will run a real GA search on the new 126-query set (100 tuning / ~26
permanent holdout, per the `md5(query) % 5` split) and compare the
champion against the currently-deployed weighted-fusion config
(alpha=0.535) on that new holdout, deploying automatically only if it
clears the 0.5-percentage-point margin. Not run manually in this entry --
letting the cron do its job is the whole point of having built it, though
the actual P2 percentage on this larger set hasn't been re-measured here
and is worth checking after tomorrow's run lands.
