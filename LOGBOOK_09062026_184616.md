# Logbook — TODO.md/MASTER.md/LOGBOOK.md created (didn't exist); Step 9 (comparative benchmark) added to the plan

**2026-09-06, ~18:46 CEST** (`date -u` ~16:46)

## What was checked first

`ls` at the repo root for `TODO.md`, `MASTER.md`, `LOGBOOK.md` — **none
existed.** Only the 24 timestamped per-change `LOGBOOK_mmddyyyy_hhmmss.md`
files were present (2026-09-02 through 2026-09-06). Confirmed, not
assumed, before deciding to create rather than update.

## What was created

All three, populated with real, live-checked state at creation time --
not placeholders:

- **`TODO.md`** -- Steps 1-9 with current status re-verified live for
  this entry (not restated from earlier in-chat report text): query_log
  count re-checked (14/0, was 11/0 a few hours earlier the same day),
  research candidate #1's real pending/decision-date re-confirmed, P7
  batch's escalating slowness re-checked (73+ min on query 1, up from
  51 min at the last check a few hours earlier -- flagged as worth
  closer attention, not silently carried forward as "in progress"),
  Elasticsearch/OpenSearch absence verified live (`docker ps`, `ss
  -tlnp` on ports 9200/9201) rather than assumed for Step 9's own
  prerequisites list.
- **`MASTER.md`** -- infrastructure facts re-verified, not copied from
  memory: confirmed live that sensalis-node's checkout has **no `.git`
  directory at all** (`git log` returns `fatal: not a git repository`)
  -- consistent with this project's own documented repo/execution split
  (tar-shipped, not git-cloned), but worth stating explicitly since the
  user's request assumed commit history might be checkable on the node.
  Phase table (P0-P7) cross-checked against today's real numbers rather
  than restating README's own status line uncritically.
- **`LOGBOOK.md`** -- root index, backfilled with all 24 existing
  timestamped entries, newest first, each row's title pulled directly
  from that file's own first line (`head -1`) rather than paraphrased
  from memory -- several of the earlier (Sep 2 - early Sep 3) entries
  predate this session's own context, so using the file's own words
  avoids misrepresenting work this session didn't do.

## Step 9 added: comparative benchmark

Two dimensions, three targets, per the approved plan:
- **Dimension A (relevance)**: this engine vs. SearXNG (already running
  on this node, no new hosting needed -- corrected the plan's default
  assumption after checking) vs. Elasticsearch/OpenSearch (needs
  provisioning, confirmed not present) vs. Google/Bing API (flagged in
  advance as likely blocked, same category as Step 4's Microsoft Graph
  blocker -- not yet actually checked whether credentials exist, that's
  Step 9's own first task when it starts).
- **Dimension B (speed/resources)**: explicit, pre-disclosed hardware
  mismatch -- sensalis-node (2-core Celeron) is not comparable to the
  CCX23 production target (4 dedicated vCPU), a gap already established
  throughout this project's history (e.g. P2's node-vs-laptop
  cross-platform variance). Any Step 9 numbers measured on sensalis-node
  must say so, not present as CCX23-equivalent.
- **Status: not started.** No benchmark harness, no Elasticsearch/
  OpenSearch instance, Google/Bing credential status unchecked. Marked
  explicitly as not blocking Steps 1-8, per the plan.

## Files changed

- `TODO.md` -- created.
- `MASTER.md` -- created.
- `LOGBOOK.md` -- created (this entry is its first addition beyond the
  backfill).
- This file.

No code changed in this entry -- documentation/planning only.
